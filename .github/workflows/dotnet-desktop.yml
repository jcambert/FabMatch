# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow will build, test, sign and package a WPF or Windows Forms desktop application
# built on .NET Core.
# To learn how to migrate your existing application to .NET Core,
# refer to https://docs.microsoft.com/en-us/dotnet/desktop-wpf/migration/convert-project-from-net-framework
#
# To configure this workflow:
#
# 1. Configure environment variables
# GitHub sets default environment variables for every workflow run.
# Replace the variables relative to your project in the "env" section below.
#
# 2. Signing
# Generate a signing certificate in the Windows Application
# Packaging Project or add an existing signing certificate to the project.
# Next, use PowerShell to encode the .pfx file using Base64 encoding
# by running the following Powershell script to generate the output string:
#
# $pfx_cert = Get-Content '.\SigningCertificate.pfx' -Encoding Byte
# [System.Convert]::ToBase64String($pfx_cert) | Out-File 'SigningCertificate_Encoded.txt'
#
# Open the output file, SigningCertificate_Encoded.txt, and copy the
# string inside. Then, add the string to the repo as a GitHub secret
# and name it "Base64_Encoded_Pfx."
# For more information on how to configure your signing certificate for
# this workflow, refer to https://github.com/microsoft/github-actions-for-desktop-apps#signing
#
# Finally, add the signing certificate password to the repo as a secret and name it "Pfx_Key".
# See "Build the Windows Application Packaging project" below to see how the secret is used.
#
# For more information on GitHub Actions, refer to https://github.com/features/actions
# For a complete CI/CD sample to get started with GitHub Action workflows for Desktop Applications,
# refer to https://github.com/microsoft/github-actions-for-desktop-apps

name: .NET Core Desktop

on:
  push:
    branches: [ "claude/saas-sheet-metal-matchmaking-RCWBa" ]
  pull_request:
    branches: [ "claude/saas-sheet-metal-matchmaking-RCWBa" ]

jobs:

  # ---------------------------
  # 1) Tests d’intégration Linux
  # ---------------------------
  tests:
    runs-on: ubuntu-latest

    env:
      Test_Project_Path: tests/FabMatch.Tests/FabMatch.Tests.csproj

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Start PostgreSQL container
      run: |
        docker run -d \
          -p 5432:5432 \
          -e POSTGRES_PASSWORD=postgres \
          --name pg \
          postgres:15.17-trixie

    - name: Wait for PostgreSQL
      run: |
        for i in {1..20}; do
          if pg_isready -h localhost -p 5432; then
            echo "PostgreSQL is ready"
            exit 0
          fi
          echo "Waiting for PostgreSQL..."
          sleep 2
        done
        echo "PostgreSQL did not start in time"
        exit 1

    - name: Run integration tests
      run: dotnet test ${{ env.Test_Project_Path }} --logger trx

  # ---------------------------
  # 2) Build Windows Desktop + MSIX
  # ---------------------------
  build:
    runs-on: windows-latest
    needs: tests

    strategy:
      matrix:
        configuration: [Debug, Release]

    env:
      Solution_Name: FabMatch.sln
      Wap_Project_Directory: src/FabMatch.Web
      Wap_Project_Path: src/FabMatch.Web/FabMatch.Web.csproj

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Restore solution
      run: msbuild $env:Solution_Name /t:Restore /p:Configuration=${{ matrix.configuration }}

    - name: Decode signing certificate
      run: |
        $bytes = [System.Convert]::FromBase64String("${{ secrets.Base64_Encoded_Pfx }}")
        $path = Join-Path $env:Wap_Project_Directory "GitHubActionsWorkflow.pfx"
        [IO.File]::WriteAllBytes($path, $bytes)

    - name: Build MSIX package
      run: msbuild $env:Wap_Project_Path `
            /p:Configuration=${{ matrix.configuration }} `
            /p:UapAppxPackageBuildMode=StoreUpload `
            /p:AppxBundle=Always `
            /p:PackageCertificateKeyFile=GitHubActionsWorkflow.pfx `
            /p:PackageCertificatePassword=${{ secrets.Pfx_Key }}

    - name: Remove certificate
      run: Remove-Item "$env:Wap_Project_Directory\GitHubActionsWorkflow.pfx"

    - name: Upload MSIX artifacts
      uses: actions/upload-artifact@v4
      with:
        name: MSIX Package
        path: ${{ env.Wap_Project_Directory }}\AppPackages

