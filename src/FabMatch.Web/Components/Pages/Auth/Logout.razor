@page "/auth/logout"
@inject NavigationManager Nav

<MudText Typo="Typo.body2" Class="pa-4">Signing out...</MudText>

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/auth/logout/execute", forceLoad: true);
    }
}
